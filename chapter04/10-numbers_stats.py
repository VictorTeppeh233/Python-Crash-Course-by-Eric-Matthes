#Simple Statistics with a List of Numbers

digits = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]

print(f"minimum digit is {min(digits)}")

print(f"maximum digit is {max(digits)}")

print(f"sum of digits is {sum(digits)}")